// Keep this file so expo-router ignores it by exporting a component.
// All API helpers are defined in ../constants/api.
export { api, saveToken, clearToken, getToken } from "../constants/api";

export default function ApiPlaceholder() {
  return null;
}
